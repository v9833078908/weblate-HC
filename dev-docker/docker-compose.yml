# Copyright © Michal Čihař <michal@weblate.org>
#
# SPDX-License-Identifier: GPL-3.0-or-later

##########
# WARNING!
# This docker-compose file is meant to be used in the development process
# and WILL perform very poorly in production.
#
# For production-ready docker-compose file see:
# https://github.com/WeblateOrg/docker-compose
#########

services:
  cache:
    image: redis:8.10.0-alpine@sha256:978f0e01593e65eed801f2402944efcd936d43b5027e4908a7897baf88ed6241
    volumes:
    - redis-data:/data
    command: [redis-server, --save, '60', '1']
    restart: always
  database:
    image: postgres:18.4-alpine@sha256:9a8afca54e7861fd90fab5fdf4c42477a6b1cb7d293595148e674e0a3181de15
    volumes:
    - postgres-data:/var/lib/postgresql
    - ./postgres-init:/docker-entrypoint-initdb.d
    env_file:
    - ./environment
    ports:
    - 5434:5432
    restart: always
  maildev:
    image: maildev/maildev:2.2.1@sha256:180ef51f65eefebb0e7122d8308813c1fd7bff164bc440ce5a3c2feee167a810
    ports:
    - 1080:1080
  weblate:
    image: weblate-dev:latest
    build:
      context: ./weblate-dev/
      args:
        USER_ID: ${USER_ID}
        GROUP_ID: ${GROUP_ID}
    depends_on:
    - cache
    - database
    - maildev
    volumes:
      # Local data directory
    - $PWD/data:/app/data   # Override Weblate with local dir
    - $PWD/..:/app/src
    environment:
      DJANGO_SETTINGS_MODULE: weblate.settings_docker
      WEBLATE_SITE_DOMAIN: ${WEBLATE_HOST}
      WEBLATE_CLARITY_PROJECT_ID: ${WEBLATE_CLARITY_PROJECT_ID:-}
      WEBLATE_EMAIL_HOST: maildev
      WEBLATE_EMAIL_PORT: 1025
      WEBLATE_EMAIL_USE_TLS: 0
      WEBLATE_EMAIL_USE_SSL: 0
      WEB_WORKERS: 1
      CELERY_TRANSLATE_OPTIONS: --concurrency 2 --prefetch-multiplier 1
      WEBLATE_VCS_ALLOW_SCHEMES: https,ssh,git,file,http
      WEBLATE_VCS_ALLOW_HOSTS: 192.168.0.3,corp.herocraft.com,83.219.159.2
      GRANIAN_ARGS: --reload --reload-paths /app/src/weblate
      WEBLATE_ADD_MACHINERY: weblate_customization.machinery.RoutedLLMTranslation
      WEBLATE_ADD_CHECK: weblate_customization.checks.GameMarkupCheck,weblate_customization.checks.GameLineBreakCheck,weblate_customization.checks.CyrillicLeakCheck,weblate_customization.checks.GameNumberCheck,weblate_customization.checks.GameTokenCheck,weblate_customization.checks.GameLengthCheck
      WEBLATE_ADD_AUTOFIX: weblate_customization.autofixes.LineSeparatorSpacing,weblate_customization.autofixes.RemoveAddedFinalStop,weblate_customization.autofixes.AddFrenchPunctuationSpacing
      WEBLATE_JUDGE_ENABLED: 1
      WEBLATE_JUDGE_MODEL_SEAT_1: deepseek/deepseek-v4-pro
      WEBLATE_JUDGE_MODEL_SEAT_2: qwen/qwen3-235b-a22b-2507
      WEBLATE_JUDGE_MAX_REPAIR_ATTEMPTS: 1
    env_file:
    - ./environment
    ports:
    - ${WEBLATE_PORT}:8080
    restart: always
    user: $USER_ID:$GROUP_ID
    tmpfs:
    - /app/cache:exec,uid=$USER_ID,gid=$GROUP_ID
    - /tmp:uid=$USER_ID,gid=$GROUP_ID,exec
    - /run:uid=$USER_ID,gid=$GROUP_ID
    userns_mode: host
volumes:
  weblate-data: {}
  postgres-data: {}
  redis-data: {}
