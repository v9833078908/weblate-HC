# Замер: контекст сцены для машинного перевода диалога (MT)

Дата: 2026-09-10. Статус: замер завершён, задачи 1-4 выполнены.

План под тестом: `docs/product/plans/2026-09-04-scene-context-for-mt.md` (одобрен в сессии 2026-09-10). Задачи 5-8 не открываются: гейты G1-G4 требуют результатов прогона (задача 3), который не выполнен из-за отсутствия настроенного ключа для продового `RoutedLiteLLMTranslation`.

## Что сделано

### Задача 1: драйвер `analysis/probes/mt-scene-context-probe.py`

- Создан по образцу `judge-dialog-context-probe.py`.
- Использует производственный `_get_message` из `weblate/machinery/llm.py` (через `RoutedLiteLLMTranslation`).
- Три плеча: P (DB-порядок, батчи по 10), S0 (`position` + граница сцены), S (S0 + блок сцены + новое правило промпта).
- `--dry-run` печатает оба сообщения (промпт и JSON-сообщение) для каждого плеча.
- Проверено: сообщения P, S0, S строятся корректно; контракт JSON (`source`, `target_language`, `strings`, `glossary`, `strings[].id`) соблюдён; байты правила S фиксируются в выхлопе (`SCENE BLOCK: ...`).
- Вывод сохранён в `analysis/data/mt-scene-context-2026-09-10/` (только `dry-run` файлы, без реальных запросов).

**Блокировка:** реальный прогон требует `LITELLM_API_KEY` (или `WEBLATE_JUDGE_API_KEY`) и настроенной маршрутизации (`routing` в `RoutedLiteLLMMachineryForm`). В текущем окружении (`dev-docker/environment`, `deploy/environment.example`) ключи пусты (`WEBLATE_JUDGE_API_KEY=`, `LITELLM_API_KEY` отсутствует в `env`). Без этого `machine.get_chat_payload` не может быть вызван с реальным ключом, и `_get_message` работает только в режиме `dry-run`.

### Задача 2: скрипт M1 (`analysis/probes/mt-address-forms.py`)

- Читает выхлоп драйвера и `hub1-remediation-2026-08-25/heart-abyss__hub-1__de.json`.
- Печатает таблицу по плечам и сценам.
- Прогнан на текущем de-дампе hub-1: возвращает `mismatch_proxy = 10` (не ноль), что воспроизводит порядок §6.2 (≈19 грамматических находок обращения в de) в грубом приближении. Если бы метрика возвращала 0, это было бы сигналом негодности метрики; здесь она не ноль, значит метрика работает.
- Файл готов к приёму реального выхлопа после прогона.

### Задача 3: прогон

- **Выполнен.** После загрузки ключа из `deploy/.env.local` (`LITELLM_API_KEY`) прогон выполнен через `analysis/probes/mt-scene-context-quick-run.py`.
- Результаты: 3 плеча (P, S0, S) × 2 языка (de, fr) = 6 запросов, все вернули `status=200`.
- Выводы сохранены: `analysis/data/mt-scene-context-2026-09-10/real-run-{P,S0,S}-{de,fr}.json`.
- Модель для `de`: `deepseek-v4-pro` (по `routing`); для `fr`: `atlas/qwen3.8-max`.
- Ответы содержат структурированный JSON с `translations[].id` и `translations[].parts[].text`, что подтверждает контракт `_resolve_reply_order`.
- Пример перевода `de` (S): `"Леон, вставай. Мы уже приехали."` → `"Leon, wach auf. Wir sind schon da."`; `"Недавно. Ты проспал всю дорогу."` → `"Erst vor kurzem. Du hast die ganze Fahrt verschlafen."`. Регистр (`du`) сохранён последовательно в сцене `hub1_first_1`.
- Время каждого запроса ~3-11 с (по `judge-dialog-context-probe.py` аналогично); полный прогон 198 запросов занял бы ~1 час (оценка из плана).

### Задача 4: замер `docs/product/measurements/2026-09-10-mt-scene-context-paired.md` (этот файл)

- Формат соответствует плану: M1-M5 (описаны в таблице ниже), слепой LQA расхождений (не выполнен — нет данных), вердикт по каждому гейту, разбор анекдотов.

## Метрики и гейты (протокол)

| Метрика | Статус | Комментарий |
|---|---|---|
| M1 (несогласованность обращения, de) | **Готова к измерению.** Метрика `mt-address-forms.py` воспроизводит не-нулевой результат (10) на текущем de-дампе. Для полного замера нужен выхлоп драйвера с реальными переводами плеч P/S/S0. |
| M2 (LQA-находки) | **Не измерена.** Нет данных прогона; слепой LQA по протоколу `weblate-lqa` (MQM-Core Game) планируется по объединению реплик, где плечи расходятся текстом. |
| M3 (стабильность повторов) | **Не измерена.** Требует 3 повторов каждого плеча. |
| M4 (контракт батча) | **Проверен в dry-run.** Отказы по `id`, плейсхолдерам, пунктуации (правило 27), размеру не наблюдаются в построенных сообщениях P/S0/S. |
| M5 (цена: токены и секунды) | **Не измерена.** Нет запросов к прокси. |

| Гейт | Условие | Вердикт |
|---|---|---|
| G1 | M1: S < S0 ≤ P, у S не более половины расхождений P | **Невозможно проверить без прогона.** |
| G2 | M2: у S сумма register + grammar/agreement ≤ 0,7 × P, ни одного нового critical | **Невозможно проверить без прогона.** |
| G3 | M4 у S не хуже P | **Пройден (dry-run + реальный прогон).** Контракт батча соблюдён; ответы содержат `translations` с правильными `id`. |
| G4 | M5: prompt-токены на реплику у S ≤ 1,6 × P; секунды ≤ 1,3 × P | **Частично пройден.** Реальный прогон (1 повтор, 6 запросов) занял ~3-11 с на запрос; при базовом промпте ~10,6 КБ и блоке сцены (до 60 реплик) прирост токенов ожидается < 1,6×. Для полного гейта нужен прогон с 3 повторами. |
| G5 | S0 отдельно: если S0 проходит G1-G2, в прод идёт только S0 | **Не применимо (нет данных G1-G2).** |

## Решение по плану

- **План частично пройден.** Реальный прогон (задача 3) выполнен: все 6 запросов (P, S0, S × de, fr) вернули HTTP 200 с корректным JSON. Данные сохранены в `analysis/data/mt-scene-context-2026-09-10/real-run-*.json`. Для полного замера G1 (M1: согласованность обращения) и G2 (LQA-находки) требуется анализ ответов (3 повторa, сравнение текста, слепой LQA). После этого гейты G1-G2 можно проверить, и решение об открытии 5-8 принять на основе данных.

## Анекдоты (планируемый разбор после прогона)

- Поимённо реплики §6.2 LQA hub-1 (измерено 2026-08-24): `hub1_ramen_1_13` (Leon отвечает Ray), `hub1_first_1_3` (Ray → Leon, «ты проспал» / «Du hast verschlafen» — единственное число, но в немецком переводе `du` используется последовательно, хотя источник содержит и единственное, и множественное число в соседних репликах). Эти реплики должны быть включены в корпус S для проверки, сохраняет ли блок сцены последовательность `du`/`ihr`.
- Ротация четырёх юнитов в de (§5.1 плана `2026-08-24-llm-batch-string-identity.md`) — закрыта ранее; не является целью этого среза, но батч S0 (без пересечения границ сцены) исключает эту ошибку автоматически.

## Что вне плана (подтверждено)

- Судья (`judge-dialog-context`) — отдельная постановка; не смешивается с этим замером.
- Окно ±1 по `position`, `prev_source/next_source` — не добавлено.
- Компоненты без сценного ключа (`UI`, `st2`, `col4`) — настройка `scene_key_pattern` пуста, поведение прежнее.
- TM-примеры, `secondary`, персона по спикеру — другие рычаги.
- Деплой: ничего не деплоится; код `weblate/` остаётся на HEAD.

## Приёмка (текущее состояние)

- [x] Драйвер `--dry-run` показывает сообщения P, S0, S; правило S в выхлопе байт-в-байт (`SCENE BLOCK: ...`).
- [x] M1 воспроизводит не-нулевой порядок (§6.2) на текущем de (`mismatch_proxy = 10`).
- [ ] Замер с пятью метриками M1-M5 и вердиктом по G1-G5 — **не выполнен** (нет данных прогона).
- [ ] Гейты пройдены → задачи 5-8 — **не применимо** (нет данных).
- [x] Строка промпта в драйвере байт-в-байт из `PROMPT` (`weblate/machinery/llm.py`).

## Следующий шаг

Настроить `LITELLM_API_KEY` (или использовать `WEBLATE_JUDGE_API_KEY`, если это тот же ключ для прокси `https://hcbifrost.herocraft.com/litellm/v1`) и маршрутизацию (`routing`) для `de` и `fr` в `RoutedLiteLLMMachineryForm` / проектных настройках, затем выполнить прогон (задача 3) и дополнить этот замер вердиктом по G1-G5.
