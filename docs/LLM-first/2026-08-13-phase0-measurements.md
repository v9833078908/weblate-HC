# Замеры фазы 0: A2, калибровка каскада B2', дрейф повторов C

Дата: 2026-08-13. Статус: **A2 остановлен по предписанию, B2' и C выполнены
на незапечатанных сплитах.** Коммит кода на момент замеров: `de58138`.
Среда всех прогонов — `dev-docker-weblate-1`, прод не затрагивался.
Фактическая трата OpenRouter: **$3.40** при бюджете $8-15.

Этот документ заменяет запланированный
`docs/LLM-first/2026-08-XX-judge-calibration.md`: он покрывает все три
измерения фазы 0, а не только калибровку.

Основание и постановка задач:

- `docs/LLM-first/plans/2026-08-11-phase0-schema-and-judge-calibration.md`
  (треки A, B, C)
- `docs/LLM-first/plans/2026-08-12-phase0-implementation.md`
  (задачи 2, 3, 4)
- `docs/LLM-first/2026-08-13-judge-native-ui-design.md` (лестница
  эскалации, роли ступеней, риски)

Инструменты, отгруженные этой работой:

| Файл | Что делает |
|---|---|
| `docs/misc/col4-judge-eval.py` | калибровка судей: плечи `cascade` / `mirror` / `single`, режимы `--probe` и `--dry-run` |
| `docs/misc/col4-repeat-drift-probe.py` | пробник дрейфа повторов, read-only, без LLM |

Результаты в машинночитаемом виде: `docs/misc/col4-judge-probe.json`,
`col4-judge-screen-dev.json`, `col4-judge-single-traindev.json`,
`col4-judge-{cascade,mirror}-ds-{sonnet,qwen}.json`,
`col4-repeat-drift.json`, запись `after[3]` в `col4-eval-baseline.json`.

## Главный вывод одной строкой

**Каскад из двух ступеней проигрывает одиночному судье на всех четырёх
измеренных конфигурациях, и проигрывает структурно, а не по невезению.**
Одиночный `deepseek/deepseek-v4-pro` даёт recall 94.0% и специфичность
96.8% при цене $0.28 на 1000 строк; лучший каскад даёт recall 87.1% при
специфичности 98.4%. Причина измерена прямо: ступень A ошибается редко,
поэтому у ступени B почти нет ложных тревог для снятия, и её
переголосования снимают настоящие дефекты — 46 из 48 в худшей
конфигурации.

---

## Трек A2. Снимок no-regression — остановлен

### Что произошло

Задача 2 impl-плана предписывала прогнать `docs/misc/col4-eval-harness.py`
внутри dev-контейнера на `col4/data/fr` и остановиться, если fingerprint
выборки не совпадёт с историческим `4db4b89dfd4b`. Он не совпал:

```text
fingerprint             00fbe7a86b65
fingerprint ожидаемый   4db4b89dfd4b
fingerprint по source   4dcb05628599
```

Сравнение не проводилось, вердикт no-regression не выносился. Снимок
записан в `docs/misc/col4-eval-baseline.json` как четвёртая запись `after`
с `_environment: "dev-docker mirror"` и собственным полем `_status`,
чтобы его нельзя было прочитать как вторую точку на той же кривой.

### Почему совпадения не могло быть в принципе

`fingerprint()` хэширует `unit.id` вместе с `unit.source`
(`col4-eval-harness.py:110`). Идентификаторы юнитов локальны для базы, а
все исторические отпечатки — в этом файле, в `col4-model-eval.json` и в
`col4-batch-size-eval.json` — посчитаны против прода. Зеркало несёт те же
3941 юнит с непрерывными id 177675-181615 и тот же шаг выборки 26, то есть
логика отбора идентична, но тождество выборки этим хэшем через две базы не
выражается. Ни один прод-идентификатор в репозитории не записан, поэтому
расхождение неразрешимо офлайн.

### Второе расхождение, которое обесценило бы сравнение и при совпадении

Глоссарий зеркала несёт 153 записи против 22 канонических терминов на
проде — известное и задокументированное различие
(`plans/2026-08-11-phase0-schema-and-judge-calibration.md:167-168`). На той
же выборке оно поднимает число глоссарных вхождений с 16 до 68 и
раздувает каждый промпт:

| Показатель | Прод, batch 10 (`col4-model-eval.json`) | Зеркало, 2026-08-13 |
|---|---|---|
| prompt_tokens | 67 828 | 178 038 |
| completion_tokens | 8 603 | 92 872 |
| cost_usd | 0.0348 | 0.2813 |
| requests | 15 | 17 |
| length_ratio_avg | 1.274 | 1.418 |
| coverage | 100% | 100% |
| glossary adherence | 100% | 100% |
| glossary occurrences | 16 | 68 |

Побочно вскрылась разница слагов: прод-глоссарий `glossariy`, зеркальный —
`glossary`; прогон запускался с `EVAL_GLOSSARY=glossary`.

### Что сделает A2 отвечаемым

1. Сделать тождество выборки переносимым: хэшировать
   `(unit.context, unit.source)` вместо `(unit.id, unit.source)`. `context`
   — это msgctxt, он стабилен между базами. Это один раз переименует все
   записанные отпечатки, поэтому измерительная задача не стала делать
   этого своей властью.
2. Либо отказаться от межсредовой амбиции и гонять гейт dev-против-dev:
   то же зеркало на историческом коммите и на HEAD, подряд. Это
   контролируемый A/B; нынешняя конструкция им не является.

Решение нужно принять до следующей попытки A2; сам по себе прогон ничего
не стоит.

---

## Трек B2'. Калибровка судей

### Что и на чём мерялось

Золотой набор `col4-judge-golden.json`, 919 записей. Использованы
**train + dev** (486 записей, из них 3 удержаны как few-shot, осталось
483: clean 251, terminology 118, mutation 114; 232 дефекта против 251
чистой). Скрининг моделей шёл на dev (376 записей).

**Сплит `test` (433 записи) не тронут.** Ни один прогон его не читал;
CLI отказывается смешивать его с другими сплитами. Единственный
go/no-go прогон по нему остаётся впереди и требует отдельного решения.

Контракт запроса: строгий structured output (`strict: true`), корень
схемы — объект `{segments: [{id, errors: [{span, category, severity}],
verdict}]}`, `provider.require_parameters: true`, батч 10, concurrency 4.
Вердикт выводится из максимальной severity в коде, а не берётся из поля
`verdict` модели — инвариант 4 архитектуры; расхождение между двумя
считается отдельным счётчиком и оказалось пренебрежимым (0 у
deepseek/haiku/command-a/gpt-5.4-mini, 1 у qwen, 8 у sonnet-5).

Неразобранный или неполный ответ считается сбоем транспорта, никогда не
вердиктом: один ретрай, затем запись помечается `unparsed` и не попадает
ни в flag, ни в reject.

### Пробник схемы: `temperature` несовместим с `require_parameters`

Первый прогон пробника отсеял три модели из семи с HTTP 404 `No endpoints
found that can handle the requested parameters`. Изоляция параметра по
матрице из четырёх вариантов дала однозначный ответ:

| Модель | schema+require+temp | schema+require | schema+temp | schema |
|---|---|---|---|---|
| `claude-sonnet-5` | **404** | OK | OK | OK |
| `gpt-5.4-mini` | **404** | OK | OK | OK |
| `gpt-5.4-nano` | **404** | OK | OK | OK |
| `claude-haiku-4.5` | OK | OK | OK | OK |

Reasoning-тиры не декларируют поддержку `temperature`, и при
`require_parameters: true` роутер не находит эндпоинта. `temperature`
убран из payload: гарантия соблюдения схемы (урок Cathedral — 19.6%
потерянных вердиктов на свободном тексте) важнее номинального нуля
температуры, который reasoning-модели всё равно игнорируют. Цена решения:
прогоны не побитово воспроизводимы между провайдерами.

После правки все семь моделей приняли payload. Это ровно тот класс
отказов, который в `col4-model-eval.json` в прошлый раз стоил исключения
`gpt-5-mini` и `claude-haiku-4.5` из сравнения; пробник за $0.02 закрыл
вопрос до траты на калибровку.

### Скрининг: семь моделей поодиночке на dev (376 записей)

| Модель | recall H | спец. H | recall R | ложный critical | unparsed | $ |
|---|---|---|---|---|---|---|
| `deepseek/deepseek-v4-pro` | **96.3** [92.5, 98.2] | **96.8** [93.2, 98.5] | **100** [92.9, 100] | 0.0 [≤2.0] | 0 | 0.145 |
| `qwen/qwen3-235b-a22b-2507` | 88.8 [83.4, 92.5] | 91.5 [86.7, 94.7] | 96.0 [86.5, 98.9] | 0.0 [≤2.0] | 0 | **0.016** |
| `cohere/command-a` | 87.7 [82.2, 91.7] | 86.2 [80.6, 90.4] | 72.0 [58.3, 82.5] | 0.0 [≤2.0] | 0 | 0.267 |
| `openai/gpt-5.4-mini` | 84.5 [78.6, 89.0] | 91.5 [86.7, 94.7] | 90.0 [78.6, 95.7] | 0.5 [≤2.9] | 0 | 0.085 |
| `anthropic/claude-haiku-4.5` | 84.0 [78.0, 88.5] | 88.4 [83.0, 92.2] | 100 [92.9, 100] | 2.6 [≤6.0] | 0 | 0.142 |
| `openai/gpt-5.4-nano` | 70.6 [63.5, 76.8] | 84.7 [78.8, 89.1] | 52.0 [38.5, 65.2] | 1.1 [≤3.8] | **10** | 0.025 |
| `anthropic/claude-sonnet-5` | 69.5 [62.6, 75.7] | **100** [98.0, 100] | 98.0 [89.5, 99.6] | 0.0 [≤2.0] | 0 | 0.550 |

Граница H — «нужен человек» (flag или reject против pass). Граница R —
«есть critical» (reject против остального). Скобки — Wilson 95%.

Три вещи, которые стоит прочитать отдельно:

- **Цена не предсказывает качество.** Самая дорогая модель выборки
  (`sonnet-5`, $0.550) имеет худший recall после `nano`; самая дешёвая
  (`qwen`, $0.016, в 34 раза дешевле) обходит её на 19 пунктов recall.
- **`sonnet-5` — не плохой судья, а судья другой роли.** Специфичность
  100% при нулевом ложном флаге на 189 чистых строках — это предельная
  точность. Он просто почти ничего не находит.
- **`nano` выбывает по транспорту**: единственная модель с
  неразобранными ответами (10 записей).

### Контроль family bias

Разметчиков у набора четыре: Fable (Anthropic frontier) на исходных 260,
`openai-codex-gpt-5.6-terra` на top-up из 300, плюс две конструктивные
страты (генератор мутаций и глоссарный пробник), от разметчика не
зависящие. Гипотеза плана: судья может выигрывать на страте своего
семейства.

**Гипотеза не подтвердилась, и отклонение идёт в обратную сторону.**
Специфичность на чистых строках по разметчику:

| Судья | Fable-размеченные (n=65) | Codex-размеченные (n=124) |
|---|---|---|
| `claude-haiku-4.5` | 81.5 | 91.9 |
| `claude-sonnet-5` | 100 | 100 |
| `deepseek-v4-pro` | 95.4 | 97.6 |
| `qwen3-235b` | 95.4 | 89.5 |
| `gpt-5.4-mini` | 93.8 | 90.3 |

Anthropic-судья строже именно на Anthropic-размеченной страте, а не
мягче. Правдоподобное объяснение — не семейство, а состав: исходные 260
строк длиннее и содержат ту самую фрагментацию реалий, которую B0
отметил как доминирующий класс дефекта. Вывод для выбора: ограничение
«ступени A и B из разных семейств» остаётся в силе как принцип, но
измерением оно здесь не подпирается и не подрывается.

### Каскады на train+dev (483 записи)

| Плечо | A | B | recall A | эскалация | recall сквозной | спец. сквозная | ложный critical | $/1000 |
|---|---|---|---|---|---|---|---|---|
| одиночный | `deepseek-v4-pro` | — | — | — | **94.0** [90.1, 96.4] | 96.8 [93.8, 98.4] | 0.4 [≤2.2] | 0.284 |
| одиночный | `qwen3-235b` | — | — | — | 89.2 [84.6, 92.6] | 92.0 [88.0, 94.8] | 1.2 [≤3.5] | **0.046** |
| одиночный | `gpt-5.4-mini` | — | — | — | 87.5 [82.6, 91.2] | 92.4 [88.5, 95.1] | 1.6 [≤4.0] | 0.221 |
| mirror | `qwen3-235b` | `deepseek-v4-pro` | 90.5 | 47.6% | 87.1 [82.1, 90.8] | 98.4 | 0.0 | 0.204 |
| cascade | `deepseek-v4-pro` | `qwen3-235b` | 92.7 | 45.8% | 84.9 [79.7, 88.9] | 98.8 | 0.0 | 0.306 |
| mirror | `claude-sonnet-5` | `deepseek-v4-pro` | 78.4 | 38.1% | 77.2 [71.3, 82.1] | 100 | 0.0 | 1.518 |
| cascade | `deepseek-v4-pro` | `claude-sonnet-5` | 90.5 | 43.9% | 70.7 [64.5, 76.2] | 100 | 0.0 | 1.223 |

Одиночный `deepseek-v4-pro` бьёт каждый каскад по сквозному recall, и
бьёт три из четырёх по цене.

### Почему каскад проигрывает: механизм измерен, а не предположен

Дизайн исходил из того, что ступень A настраивается агрессивно, а ложные
тревоги снимает ступень B. Замер показывает, что первой посылки нет:
**ступень A почти не даёт ложных тревог, которые стоило бы снимать.**
Специфичность `deepseek` как ступени A — 97.6-99.2%. Соответственно
почти всё, что ступень B переголосовывает, — настоящие дефекты:

| Плечо | переголосовано | из них верно | **из них ошибочно** |
|---|---|---|---|
| cascade `deepseek` → `sonnet-5` | 48 | 2 | **46** |
| cascade `deepseek` → `qwen` | 21 | 3 | **18** |
| mirror `sonnet-5` → `deepseek` | 5 | 2 | **3** |
| mirror `qwen` → `deepseek` | 24 | 16 | 8 |

Это не свойство конкретной пары, а арифметика конструкции: если ступень B
имеет право снимать флаг, сквозной recall равен
`recall(A) × recall(B | эскалированные)` и не может превысить recall
ступени A. Каскад окупается только тогда, когда узкое место — ложные
тревоги. Здесь узкое место — пропуски, и вторая ступень их усугубляет.

Единственная конфигурация, где ступень B переголосовывает больше верно,
чем ошибочно (`qwen` → `deepseek`, 16 против 8), — это та, где сильная
модель стоит второй и фактически перепроверяет слабую. Но и она даёт
recall 87.1 против 94.0 у той же сильной модели, поставленной одной.

Отдельно стоит отметить, что каскад не бесполезен: он покупает
специфичность 98.4-100% и **нулевой ложный critical** во всех четырёх
конфигурациях. Если бы поставку блокировал ложный reject, а не пропуск,
выбор был бы обратным. Дизайн явно говорит, что это не так: ложный
critical — дефект поставки, но пропущенный дефект — то, ради чего судья
заводится.

### Пороги плана против измеренного (одиночный `deepseek-v4-pro`)

| Порог плана | Требование | Измерено на train+dev | Статус |
|---|---|---|---|
| reject-recall на critical | ≥95%, нижняя граница CI ≥88% | 100 [94.1, 100], n=61 | **пройден** |
| (flag ∪ reject)-recall на терминологии | ≥80% | 98.3 [94.0, 99.5], n=118 | **пройден** |
| false-flag на чистых | ≤10% | 2.8%, n=251 | **пройден** |
| false-reject / false-critical на чистых | ≤2% | 0.4%, верхняя граница CI 2.2% | **не сертифицируем**: даже 1 из 251 даёт верхнюю границу выше 2% |
| auto-pass | ≥90% | 53.2% на наборе; проекция на прод-пул 80.3% | **не пройден** |
| miss-rate среди auto-pass | critical ~0, major ≤1-2% | critical 0 на наборе; общая проекция 1.37% [0.71, 2.12] | пройден по проекции |

Две оговорки, без которых таблицу читать нельзя.

**Про auto-pass.** 53.2% — свойство набора, а не системы: золотой набор
намеренно обогащён дефектами (232 из 483, 48%). Перенос на прод-пул с
измеренной B0 долей дефектов 18.2% даёт auto-pass 80.3% и долю пропусков
среди auto-pass 1.37%. Это ниже цели ≥90%, и разрыв реален: судья
отправляет на разбор каждую пятую строку. Причина видна в confusion
matrix — 138 из 171 major-дефектов получают flag, и это правильно;
объём разбора задаётся не строгостью судьи, а долей дефектов в выводе
переводчика.

**Про порог false-reject.** Ограничение по мощности, зафиксированное в
B1, подтвердилось на большей выборке: 251 чистой строки всё ещё не
хватает, чтобы сертифицировать «≤2%» интервалом. Нужно ≥189 чистых
строк **при нуле ложных reject**; у нас один на 251. Порог требует либо
пересмотра, либо большей чистой страты.

### Рекомендация

1. **Ступень A: `deepseek/deepseek-v4-pro`.** Лучший recall на границе H
   (94.0), идеальный reject-recall (100), нулевой пропуск critical,
   $0.28 на 1000 строк. Семейство отлично от переводчика
   (`google/gemini-2.5-flash`), инвариант 3 соблюдён.
2. **Ступень B не добавлять** в том виде, в каком она описана в дизайне,
   то есть с правом снимать флаг. Замер говорит, что она снимает дефекты,
   а не шум.
3. Если второй сигнал всё же нужен — менять не модель, а **правило
   слияния**: вторая ступень должна уметь только повышать severity либо
   помечать разногласие для человека, но не понижать вердикт. Такая
   конструкция не может уронить recall по построению. Это отдельный
   замер, в объём фазы 0 он не входит.
4. **Дешёвая альтернатива для бюджетного сценария**: `qwen3-235b` даёт
   recall 89.2 при $0.046 на 1000 строк — в шесть раз дешевле deepseek
   и на 4.8 пункта хуже по recall. Для полного прогона COL4 fr (3941
   строка) разница абсолютная: $1.12 против $0.18.
5. **Go/no-go по запечатанному test не выдан** и не может быть выдан из
   этого документа: все числа выше получены на train+dev, по которым шли
   итерации. Рекомендуемый следующий шаг — единственный прогон
   `--arm single --split test --models deepseek/deepseek-v4-pro`
   (~$0.12), после которого итерации по test запрещены.

---

## Трек C. Дрейф не-глоссарных повторов

Прогон: `docs/misc/col4-repeat-drift-probe.py` на `col4/data/fr`
dev-зеркала, 3941 юнит, 153 глоссарных термина исключены. Read-only, без
LLM-вызовов, $0. Полный результат — `docs/misc/col4-repeat-drift.json`.

Пробник меряет двумя слоями, потому что они неравно достоверны.

### Слой A: одинаковые источники — факт

146 групп юнитов с идентичным нормализованным источником, 372 юнита
(9.4% корпуса).

| Показатель | Значение |
|---|---|
| групп | 146 |
| расходятся, raw | 106 (72.6%) |
| расходятся, после нормализации | 84 (**57.5%**) |
| юнитов в расходящихся группах | 215 |
| **видит стоковый чек `inconsistent`** | **0 (0.0%)** |
| групп, где хотя бы два юнита делят context | 0 |

Нормализация (casefold, trim, схлопывание пробелов, снятие финальной
пунктуации) снимает 22 группы из 106 — то есть пятая часть «дрейфа» была
косметикой, которую слой 0 убирает бесплатно, а четыре пятых — нет.

Примеры расхождений: `промолчать` (9 юнитов) → `garder le silence` /
`laisser la parole` / `ne rien dire` / `se taire`; `лежать и не
шевелиться` (3) → `rester allongé sans bouger` / `rester immobile`. Это
кнопки ответа в диалогах: четыре разных перевода одной кнопки — то, что
игрок видит как несогласованность.

### Посылка плана про `inconsistent` оказалась неверной

План трека C исходил из того, что «детекцию закрывает стоковый чек
`inconsistent`». Он не закрывает ничего: ноль из 84 расходящихся групп
им помечены. Причина структурная и проверяема по коду —
`Unit.objects.same()` (`weblate/trans/models/unit.py:380-388`) фильтрует
по **source И context**:

```text
result = self.filter(
    source__lower__md5=MD5(Lower(Value(unit.source))),
    context__lower__md5=MD5(Lower(Value(unit.context))),
    ...
)
```

В ключевом игровом формате у каждого юнита свой msgctxt, поэтому
`propagated_units` для внутрикомпонентных повторов всегда пусто. Это не
ошибка конфигурации и не следствие `allow_translation_propagation`:
проверено отдельно, ни в одной из 84 групп два юнита не делят context.

### Слой B: повторяющиеся подфразы — прокси

У подфразы внутри предложения нет выравнивания на целевой стороне,
поэтому «одинаково переведена» решается прокси: группа согласована, если
некоторая подстрока длиной `ratio × len(фразы)` встречается во всех
таргетах группы. Репортится при двух порогах, потому что число заметно
двигается с порогом.

5253 максимальные группы (из 9223 кандидатов после снятия вложенных
фраз с тем же следом). Разбито на страты, иначе одно число утонуло бы в
обычной лексике:

| Страта | групп | raw @0.5 | норм. @0.5 | raw @0.75 | норм. @0.75 |
|---|---|---|---|---|---|
| многословные фразы | 2627 | 13.1% | 11.8% | 32.3% | 31.0% |
| одиночные имена собственные | 26 | 23.1% | **19.2%** | 38.5% | 26.9% |
| одиночная общая лексика | 2600 | 27.6% | 25.7% | 41.6% | 39.1% |

Страта общей лексики приведена, чтобы её шум был виден, а не подмешан в
заголовок: `даже` встречается в 49 юнитах и не имеет канонического
французского перевода — считать это дрейфом значит измерять русский
язык, а не наш конвейер.

Страта имён собственных мала (26 групп) именно потому, что реалии в
основном уже в глоссарии и исключены из группировки. Оставшийся хвост —
то, что глоссарий не покрывает, и он воспроизводит доминирующий класс
дефекта, найденный B0:

- `чествование` (5 юнитов) → `défilé des bannières des ménages` /
  `cérémonie des drapeaux de maison` / `célébration des bannissements de
  la faction`
- `хриплого` (5) → `hriplog` / `hriplogo`
- `гнилушку` (3) → `l'écume` / `la pourriture` / `le pourri`

Отдельная методическая заметка: первая версия пробника относила к именам
собственным любой токен, встреченный с заглавной буквы вне начала
предложения, и страта протекла до 102 групп за счёт ALL-CAPS-заголовков
(`лаборатории`, `парень`). Признак заменён на долю титульных вхождений
(≥2 и ≥60% от всех), после чего страта сжалась до 26 честных групп.

### Вывод трека C

**Дрейф материален и не детектируется.** По правилу плана («дрейф
материален → рантайм-канон встаёт в фазу 2/4 отдельным дизайном») трек C
закрывается не «замером в ноль», а постановкой задачи. При этом дизайн
должен решать две разные проблемы, а не одну:

1. **Детекция.** 57.5% групп с идентичным источником переведены
   по-разному, и ни одна не видна ни в интерфейсе, ни в статистике.
   Самый дешёвый шаг здесь — не рантайм-канон, а чек, который сравнивает
   юниты с одинаковым источником **внутри компонента, игнорируя
   context**. Это ровно тот запрос, который `Unit.objects.same()` уже
   умеет строить, минус одно условие.
2. **Предотвращение.** Рантайм-канон first_mentions по образцу правки
   10.1 Cathedral. Порядок батчей и concurrency ломают семантику канона,
   поэтому его надо проектировать, а не копировать.

Первое дешевле второго на порядок и даёт цифру, по которой второе можно
будет оценить.

---

## Что осталось незакрытым

| Пункт | Состояние |
|---|---|
| A2 | остановлен; требует решения по переносимому fingerprint либо по dev-против-dev |
| B2' на запечатанном test | **не запускался**; go/no-go не выдан |
| Порог false-reject ≤2% | не сертифицируем при 251 чистой строке; нужен пересмотр порога или большая страта |
| Цель auto-pass ≥90% | не достигнута: проекция 80.3% |
| Ступень B с правилом «только повышать severity» | не измерялась; следует из вывода B2' |
| Чек внутрикомпонентной несогласованности | следует из вывода C; не спроектирован |
| Прод-прогон backfill автофиксов `--apply` | вне объёма этой работы, остаётся предусловием боевого прогона судьи |

## Расход

| Статья | $ |
|---|---|
| A2, два прогона харнесса | 0.31 |
| пробник схемы и изоляция параметра | 0.03 |
| скрининг семи моделей на dev | 1.23 |
| одиночные контроли на train+dev | 0.27 |
| каскад и зеркало `deepseek` / `sonnet-5` | 1.32 |
| каскад и зеркало `deepseek` / `qwen` | 0.25 |
| трек C | 0.00 |
| **итого** | **3.40** |
