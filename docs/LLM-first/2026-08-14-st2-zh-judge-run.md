# Прогон коллегии на живом компоненте: S&T2 summer-update, zh_Hans

Дата: 2026-08-14. Статус: **прогон выполнен и размечен вручную; выводы
измерены.**
Среда: хост, прямые вызовы OpenRouter; данные читались из прода
(`l10n.herocraft.com`) через REST API, только на чтение. Трата: **$0.068**.

Инструмент: `docs/misc/st2-judge-experiment.py`. Артефакты вердиктов:
`/tmp/st2_ds.json`, `/tmp/st2_qw.json` (временные, не коммитятся).

Основание:

- `docs/LLM-first/2026-08-13-judge-native-ui-design.md` (коллегия, роли)
- `docs/LLM-first/2026-08-13-phase0-measurements.md` (выбор моделей)

## Чем этот прогон не является

Разметки у компонента нет, поэтому **recall и специфичность здесь
неизмеримы**. Числа ниже говорят, что судьи *находят*, и ничего не
говорят, что они *пропускают*. Калибровка остаётся за золотым набором;
этот прогон отвечает на другой вопрос — сколько тёмной материи лежит в
корпусе, который по всем формальным признакам чист.

Второе ограничение: промпт из фазы 0 написан под ru→fr и нарративную
игру. Здесь ru→zh и военная стратегия; адаптированы только название
языка и жанровая преамбула. Перенос промпта между парами языков
измерением не подпирается.

## Объект

| Показатель | Значение |
|---|---|
| компонент | `strategy-and-tactics-2/summer-update` |
| язык | `zh_Hans`, источник `ru` |
| юнитов | 124 |
| состояние | **все 124 в state 20 (переведено)** |
| failing checks | **0** |
| approved | 0 |
| suggestions / comments | 0 / 0 |
| длина источника | 4-214 символов, среднее 55 |

То есть по всем видимым в интерфейсе признакам компонент готов к
поставке.

## Результат

| | `deepseek-v4-pro` | `qwen3-235b` | **коллегия** |
|---|---|---|---|
| pass | 109 | 94 | **85** |
| flag | 12 | 19 | **26** |
| reject | 3 | 11 | **13** |
| auto-pass | 87.9% | 75.8% | **68.5%** |
| unparsed | 0 | 0 | 0 |
| стоимость | $0.0622 | $0.0055 | **$0.0677** |
| время | 453 с | 338 с | — |

Ошибки коллегии по severity: **critical 13, major 28, minor 12**.
По категориям: mistranslation 34, terminology 8, fluency 5, register 5,
addition 1.

**31.5% строк (39 из 124) коллегия отправила бы человеку, 10.5% (13)
удержала бы от сборки.** Детерминированный слой на тех же строках молчит
полностью.

## Коллегия против одиночного судьи, на живых данных

Согласие судей — 90 из 124 (72.6%). Помечено только `deepseek` — 9
строк, только `qwen` — 24. Одиночный `deepseek` пропустил бы 24 из 39
строк, которые коллегия отправляет человеку.

Это воспроизводит механизм, измеренный на запечатанном срезе: выигрыш
коллегии даёт **несовпадение ошибок**, а не сила отдельной модели.

Отдельно стоит отметить перевёрнутый порядок цены и находок. `qwen`
дороже не в разы, а в **одиннадцать раз дешевле** ($0.0055 против
$0.0622) и при этом дал больше reject (11 против 3). Строка «второй
судья стоит 5% первого» из дизайна на этой паре подтверждается с
запасом.

## Воспроизводимость: разброс между прогонами

Первый прогон `deepseek` (тот же вход, тот же промпт) дал 117 pass / 7
flag / 0 reject; второй — 109 / 12 / 3. `temperature` в payload не
передаётся сознательно (несовместим с `require_parameters`), но
reasoning-тиры недетерминированы и без него.

Практический вывод: **разовый прогон одной модели — не измерение.**
Для продуктовых порогов нужен либо повтор, либо коллегия, где разброс
одной модели частично гасится второй.

## Что нашли: классы дефектов

Достоверные, подтверждённые обоими судьями:

1. **Системная порча шаблона укреплений** — 6 строк (24180-24182,
   24187-24189). «Провальный штурм» и «удачная оборона» отрендерены
   одинаково бессмысленно: `防御工事失败防御结果`, `防御工事成功防御结果` —
   набор слов «оборона-провал-оборона-результат». Игрок не различит
   исход боя.
2. **Непереведённый фрагмент** — 24144, «МАКС» → `MAX`.
3. **Игровой термин «ход» → «天» (день)** — 24160, 24161. В пошаговой
   стратегии это смена единицы измерения.

## Ручная разметка: что из находок настоящее

Добавлено 2026-08-14 после прогона. Каждый из 39 флагов прочитан против
источника, перевода и — где это решало вопрос — соседних строк того же
семейства. Разметка целиком:
`docs/misc/st2-zh-judge-annotations.json` (39 флагов с причиной, 14
подтверждённых дефектов). Это закрывает первую строку раздела «Что
осталось незакрытым»: у компонента теперь есть разметка.

Классы: **REAL** — дефект есть, **WEAK** — дефект есть, но severity не
та или он тривиален, **FP** — перевод корректен.

| | флагов | REAL | WEAK | FP | точность |
|---|---|---|---|---|---|
| `deepseek` | 15 | 5 | 4 | 6 | 33% |
| `qwen` | 30 | 7 | 4 | 19 | 23% |
| **коллегия** | **39** | **11** | **7** | **21** | **28%** |

**Из 11 настоящих дефектов оба судьи совпали ровно на одном.**
`deepseek` в одиночку нашёл 4, `qwen` — 6. Одиночный recall 45% и 64%
против 100% у объединения: механизм коллегии на живых данных
воспроизводится в чистом виде — работает несовпадение ошибок, а не сила
модели.

### Чего судьи не увидели

Объединённый набор дефектов уровня major и выше — 14 строк; коллегия
взяла 8. **Recall 57%.**

| Пропущено | Что не так | Видно ли в одной строке |
|---|---|---|
| 24240, 24241 | `车队` (автоколонна) против `商队` в 11 юнитах; 24158 прямо говорит про боевые корабли конвоя | нет, нужны сиблинги |
| 24200 | `幸存国家 {0}` = «выжившая страна»; сиблинг 24202 даёт верное `国家 {0} 的损失` | нет, нужен сиблинг |
| 24190 | `防御工事惨败` теряет «оборону» | да, пропуск слова |
| 24183, 24184 | «Получено урона нашими войсками» → `我军损失`: урон подменён потерями | да, подмена термина |

Из 11 дефектов, видимых в одной строке, судьи взяли 8. Из 3, требующих
сравнения с соседями, — **ноль**. Это тот же дефицит, что зафиксировал
трек C, и он не лечится ни выбором модели, ни промптом: судья видит
строку по одной.

### Калибровка severity сломана в обе стороны

Гейт дизайна держит только `critical`. На этом прогоне он удержал бы 13
строк:

| Из 13 `reject` | Строк |
|---|---|
| действительно critical (24130, 24180-24182) | 4 |
| реальные, но не critical | 5 |
| **безупречные строки, заблокированные зря** | **4** |

Заблокированы зря: `拒绝` для «Отказаться» (24233),
`累计经验与升级所需经验` (24246, где перевод ещё и починил опечатку
источника), `超过12个雷区` (24252) и целое корректное предложение про
морской десант (24171), где `deepseek` поставил critical на span длиной
во всю строку.

Одновременно **единственный блокер прогона отгрузился бы**: инверсия
ролей плейсхолдеров в 24221 получила `major`, то есть `state 20`. В игре
это отрендерится как «уровня ‹Панцергренадеры› 3-я师».

Из 26 `flag` семнадцать — чистые строки.

### Сверка со вторым разбором

Тот же компонент независимо разобран вручную (10 дефектов, 8%). Все
счётные утверждения того разбора сошлись при проверке: `商队` в 11
юнитах против `车队` в 2; 8 юнитов с ASCII-двоеточием против 18 с
полноширинным, и это ровно блок SURVIVORS/CASUALTIES/RANK_STATUS;
`我军损失` ровно в двух морских DESC.

Два расхождения, оба в пользу судей:

1. **24130 пропущен вторым разбором.** «получать {PARAM0}% к доходу
   **за {PARAM1}**» отрендерено как «**支付**{PARAM1}» — «заплатите».
   Сиблинг 24131 показывает, что `{PARAM1}` — число ходов, а не цена;
   в 24253, где параметр действительно цена, стоит верное `花费`.
   Оба судьи пометили строку critical, и оба правы.
2. **24194 назван корректным, но там перевёрнут агент.**
   `非装甲单位造成伤害` без `对` читается «небронированные наносят
   урон», тогда как источник говорит «нанесено урона **по**
   небронированным». Нашёл `deepseek`. Эллипсис в параллельной
   конструкции защитим, поэтому пункт помечен как спорный.

## Главная находка для дизайна UI

Улики судей **непригодны для адресата в том виде, в каком приходят**.
Схема ответа фазы 0 не содержит поля `description`, и вся смысловая
нагрузка ложится на `span` — фрагмент целевого текста. Продюсер целевой
язык не читает, поэтому запись вида

```text
[critical] mistranslation: 拒绝
```

не позволяет ему ни принять решение, ни оценить, судил ли судья по делу.

Требование зафиксировано в дизайн-документе:
`2026-08-13-judge-native-ui-design.md`, раздел «Контракт `description`:
адресат не читает целевой язык». Оно требует правки схемы и промпта, а
значит нового запечатанного среза.

## Эксперимент: глоссарий в руках судьи

Добавлено 2026-08-14. Три плеча на одном корпусе, отличается только
промпт. Инструмент: `docs/misc/st2-judge-experiment.py --glossary`.

| Плечо | Глоссарий в промпте | Правило |
|---|---|---|
| A | нет | — |
| B | 30 терминов | «термин по глоссарию — **корректен по определению**» |
| C | 30 терминов | «conformance **necessary but never sufficient**; judge the sentence first» |

### Плечо B было испорчено формулировкой, а не глоссарием

Первая редакция правила содержала клаузу «a target that follows the
glossary is correct by definition». Это инструкция считать корректным
всё, где термины совпали. Строки 24180-24182 (`省份{0}防御工事失败防御结果`)
состоят из глоссарных терминов `防御工事`, `进攻`, `防御`, сложенных в
кашу, — и qwen, выполняя инструкцию, понизил их с
`critical/mistranslation` до `minor/fluency`.

Клауза убрана. Результат по этим трём строкам:

| | 24180 | 24181 | 24182 |
|---|---|---|---|
| B (сломанное правило) | pass | pass | pass |
| C, прогон 1 | reject | reject | reject |
| C, прогон 2 | reject | reject | reject |

**Причина установлена: виновата была формулировка, а не подача
глоссария.** Ранее записанный здесь вывод «глоссарий в руки судье давать
нельзя» — **отозван**.

### Шум между прогонами: 16%

Плечо C прогнано дважды с побайтово идентичным входом и промптом.
**20 юнитов из 124 (16%) поменяли вердикт.** Пойманных настоящих
дефектов: 7 в первом прогоне, 10 во втором.

| Плечо (qwen) | REAL из 11 | ложных тревог |
|---|---|---|
| A, n=1 | 7 | 19 |
| B, n=1 | 8 | 11 |
| C, n=2 | **7 и 10** | **11 и 8** |

Разброс внутри одного плеча — 3 юнита по REAL и 3 по FP. Отсюда:

- **Эффект на recall неизмерим.** Разница между плечами (0-3 юнита)
  целиком лежит внутри разброса одного плеча (3 юнита). Записанное
  ранее «recall 100% → 73%» — артефакт единичного прогона, **отозвано**.
- **Эффект на ложные тревоги похож на настоящий.** Разрыв A→C
  составляет 8-11 юнитов при шуме в 3. Глоссарий снимает ложные
  тревоги: `征战历程`, `海军陆战队`, `部队新等级`, `查看师的征战历程` —
  строки, которые судья флагировал, не зная, что термин утверждён.

### Что из этого следует

**Единичный прогон судьи на этом корпусе — не измерение.** Это
касается и всех чисел выше по документу: они получены одним прогоном на
плечо и имеют ту же погрешность. Любое решение о порогах требует
повторов и явно посчитанного шумового пола.

Судье глоссарий **нужен**: без него он не может судить терминологию —
он не знает, какая форма утверждена. Нельзя другого: сообщать ему, что
соответствие глоссарию равно правильности. Это два разных вопроса.
Детерминированный `check_glossary` отвечает на первый точно и
бесплатно; судья должен отвечать на второй.

## Эксперимент: второе семейство или второй прогон

Добавлено 2026-08-14, после замера шума. Вопрос: коллегия из двух
семейств обоснована дизайном (`2026-08-13-judge-native-ui-design.md:132-138`)
тем, что «самосогласованность одной модели сбивает дисперсию, но не
смещение». Шум в 16% ставит вопрос иначе — покупает ли второе
семейство то, чего не даёт второй прогон того же судьи?

Четыре прогона плеча C: `qwen` x2, `deepseek` x2. Union по max severity.

| Конфигурация | вызовов | REAL из 14 | ложных тревог | цена |
|---|---|---|---|---|
| `qwen` x1 | 1 | 10 | 25 | $0.006 |
| `deepseek` x1 | 1 | 6-7 | 8-14 | $0.065 |
| `qwen` x2, одно семейство | 2 | **12** | 30 | $0.011 |
| `deepseek` x2, одно семейство | 2 | 8 | 16 | $0.131 |
| `qwen` + `deepseek`, два семейства | 2 | 10-12 | 26-29 | $0.070 |

Строка «два семейства» дана разбросом по всем четырём сочетаниям
прогонов. Она пересекается с однофамильной парой полностью.

**Решающий счёт — не агрегат, а поимённый список.** Дефектов, которые
`deepseek` за два прогона нашёл, а `qwen` за два прогона пропустил:
**ноль**. Обратных — четыре (24180, 24181, 24182, 24194).

Собственный шум `deepseek` — 14 юнитов из 124 (11%), того же порядка,
что у `qwen`.

Два дефекта не нашёл никто за четыре прогона: 24183 и 24184, пара
морских потерь. Оба требуют сравнения с соседней строкой — тот же класс,
что и три пропуска из разметки.

### Что это значит

Дизайн обосновывает отказ от 3x-агрегации так: «неразобранных ответов
ноль на 916 записей, то есть дисперсии транспорта, ради которой
затевалась бы агрегация, просто нет» (`:136-138`). Это дисперсия
**транспорта**. Ноль нераспарсенных ответов ничего не говорит о том,
даёт ли один и тот же вход один и тот же вердикт. Даёт разный — в 11-16%
случаев.

Аргумент «дисперсия против смещения» остаётся верным теоретически:
повторы одной модели действительно не лечат её систематический перекос.
Но на этом корпусе перекос второго семейства оказался **подмножеством**
перекоса первого, а не дополнением к нему. Второе место в коллегии
платит шестикратную цену за ноль уникальных находок.

Вывод для продукта: число мест и модели на них — конфигурация, а не
константа дизайна. Что ставить, решает замер на n=5 в плане
рекалибровки, а не текущая формулировка.

## Что осталось незакрытым

| Пункт | Состояние |
|---|---|
| Разметка этого компонента | **выполнена 2026-08-14**: `docs/misc/st2-zh-judge-annotations.json`, 39 флагов и 14 подтверждённых дефектов. Точность коллегии 28%, recall 57% |
| Пороги severity | **сломаны в обе стороны**; пересмотрены по эксперименту: предусловие — включённый `check_glossary`. План — `plans/2026-08-14-judge-severity-recalibration.md` |
| Межстрочная согласованность | 3 из 6 пропусков требуют сравнения с сиблингами; судья к этому слеп по построению. План — `plans/2026-08-14-intra-component-consistency-check.md` |
| Перенос промпта ru→fr на ru→zh | не измерен |
| Поле `description` в схеме | отсутствует; внесено как обязательное в план 1, задача 5 |
| Глоссарий | подача судье **не вредит** при верной формулировке правила (2026-08-14). Снимает 8-11 ложных тревог; эффект на recall неизмерим при шуме 16%. Детерминированный `check_glossary` — `plans/2026-08-14-intra-component-consistency-check.md` |
| Шум между прогонами | **измерен 2026-08-14**: 16% юнитов (20 из 124) меняют вердикт между идентичными прогонами qwen; REAL-детекция гуляет на 3 юнита из 11. Все однопрогонные числа этого документа имеют эту погрешность |
| Второе семейство моделей | **измерено 2026-08-14**: `deepseek` за два прогона не поймал ни одного дефекта, которого не поймал бы `qwen` за два прогона, при цене в 12 раз выше. Требование дизайна «места коллегии ходят в разные семейства» эмпирической поддержки не имеет |
