# План: фаза 0 — эксперимент json_schema и калибровка LLM-судьи

Статус: **дизайн согласован 2026-08-11** (оба трека; судьи — Anthropic
mid-tier + OpenAI mini-tier). Дополнен 2026-08-11 по итогам разбора
Cathedral (`2026-08-11-cathedral-localizer-analysis.md`, пп. 6, 10):
трек A — предусловие фазы 2, инвариант транспорта в B2, трек C.
Дополнен 2026-08-12 по разбору с eval-методологией
(hamelsmu/evals-skills: error-analysis, write-judge-prompt,
validate-evaluator): LLM-нативная разметка золотого набора (B0),
сплиты train/dev/test, CI-условия на порогах, две бинарные границы
вердикта, продуктовая метрика auto-pass/miss-rate, трек A
переквалифицирован в no-regression гейт.
Основание:
`docs/LLM-first/llm-first-product-architecture.md` (часть 5, фаза 0),
`docs/LLM-first/agent_researches/2026-08-11-llm-judge-design-research.md`,
`docs/specs/2026-08-11-llm-first-prompt-and-pipeline-review.md`
(пп. 8.2, 3.5).

Цель: закрыть последний транспортный пункт ревизии
(`response_format: json_schema`) измерением, а не верой, и получить
go/no-go для фазы 2 (судья): выбранную модель-судью, промпт и измеренные
пороги точности на размеченном наборе.

Продуктовая рамка судьи: правильно маркировать confidence перевода —
pass не требует вычитки, flag и reject уходят на вычитку. Судья обязан
сократить объём вычитки на порядок (auto-pass >= 90%) без пропуска
critical среди auto-pass.

## Трек A. `response_format: json_schema` на пути OpenRouter

### Задача A1. Код

Файлы: `weblate/machinery/openai.py`, `weblate/machinery/llm.py` (шов
парсинга), `weblate_customization/src/weblate_customization/machinery.py`,
тесты в `weblate/machinery/tests.py`.

- Схема включается только для `RoutedLLMTranslation` (OpenRouter):
  дженерик OpenAI-сервис не трогаем, часть провайдеров схему не
  поддерживает. Прецедент — `weblate/trans/loc_kit.py:425-443`, включая
  `provider.require_parameters: true`.
- Корень strict-схемы обязан быть объектом, поэтому ответ заворачивается
  в конверт `{"translations": [{"parts": [...]}, ...]}`. Парсинг
  разворачивает конверт до `_normalize_translations`; plain-массив
  остаётся принимаемым (другие сервисы, выключенная схема).
- Recovery-парсер (`_repair_json_*`) не удаляется: он общий для всех
  LLM-сервисов, на пути OpenRouter просто перестаёт срабатывать.
- Приёмка: юнит-тесты — payload OpenRouter-сервиса содержит
  `response_format.json_schema.strict` и `provider.require_parameters`;
  конверт разворачивается; payload и парсинг остальных сервисов
  байт-в-байт прежние.

### Задача A2. Замер

Инструмент: `docs/misc/col4-eval-harness.py`, та же выборка 150 строк
COL4 ru->fr (fingerprint `4db4b89dfd4b`). Среда: **dev-docker**, не прод
(A1 меняет код ядра; прод-образ не пересобирается ради эксперимента).
Плечи сравниваются только внутри одной среды.

- Плечи (4): baseline b20 | schema b20 | schema b10 | baseline b10,
  concurrency 2, back-to-back с паузой ~20 с; второй раунд через
  ~20 минут (протокол `docs/misc/col4-batch-size-eval.json`).
- Статус критерия: json_schema принимается по инварианту транспорта
  (Cathedral: 19.6% потерянных вердиктов), а не по выигрышу
  `rejects.json_error` 2 -> 0 — сигнал n=2 на 150 строках
  статистически ничтожен. A2 — гейт отсутствия регрессий, не
  доказательство пользы.
- Базлайн-плечо прогоняется 3x для фиксации дисперсии `json_error`
  (поведение OpenRouter дрейфует внутри окна замера); если базлайн сам
  колеблется 0-2, это фиксируется в выводах без претензии на
  доказанное улучшение.
- Критерии no-regression: coverage 100%; ноль unparsed на всех
  schema-плечах; `end_stop` в полосе базлайна 18-29; glossary 100%;
  cost в пределах +/-10%.
- Решение: schema без регрессий на b20 -> остаёмся на batch 20 (меньше
  запросов); регрессия на b20 -> подтверждается кандидат batch 10.
- Результат: `docs/misc/col4-schema-eval.json` (EVAL_JSON всех плеч +
  выводы), бюджет ~$0.30.

## Трек B. Калибровка судьи на золотом наборе

Standalone-скрипты без Django: ключ OpenRouter через переменную
окружения, строки выгружаются read-only через REST API прода.

Разметчик золотого набора — Fable (frontier-модель Anthropic в сессии
агента) в роли франко-нейтива: человек-переводчик на этом этапе
недоступен. Прецедент глубины разбора —
`docs/specs/2026-08-11-col4-fr-autotranslate-report.md` (п. 5).
Следствие для интерпретации: разметчик и судья Anthropic mid-tier —
одно семейство, их ошибки коррелированы, согласие этой пары на
размеченных Fable стратах может быть оптимистично завышено. Контроль:
страты с меткой по построению (глоссарные дефекты, синтетические
мутации) от разметчика не зависят; если Anthropic-судья выигрывает у
OpenAI только на Fable-размеченных стратах, но не на конструктивных —
подозревать family bias и принимать решение по конструктивным стратам.

### Задача B0. Разметка реального вывода (error analysis)

Файлы: `docs/misc/col4-judge-annotation.md` (протокол и разметка),
записи вливаются в `col4-judge-golden.json`.

**Выполнена 2026-08-12**: `docs/misc/col4-judge-annotation.md` +
`docs/misc/col4-b0-annotations.jsonl` (260 юнитов по dev-зеркалу
прод-прогона, seed 20260812: 183 pass / 77 defect; чистый пул
78.3% pass, Wilson CI [72.3%, 83.3%], 5 скрытых major; чистая страта
для B1 — 166 юнитов, цель >=150 закрыта). Дамп:
`docs/misc/col4-b0-dump.py`.

До сборки набора Fable размечает ~150-200 реальных строк прод-вывода
COL4 fr (стратифицированная выборка: случайные + строки с failing
checks + хвост самых длинных). На строку: вердикт pass/defect, MQM-тег
(terminology / accuracy / omission / fluency / punctuation / markup),
критика со ссылкой на конкретный span. Улики в контексте разметчика:
source ru, глоссарий, note, failing_checks. Два выхода:

- метки чистой страты B1: «чистая» = pass от разметчика, а не только
  0 failing checks — строка может пройти все чеки и остаться
  семантически неверной; иначе скрытые дефекты «чистой» страты
  считались бы false-flag судьи и тянули калибровку в
  снисходительность;
- сверка классов мутаций с реальностью: класс дефекта, найденный в
  разметке и отсутствующий в списке мутаций, добавляется в список;
  класс, не встреченный в реальном выводе, помечается как
  гипотетический (защита от придуманных категорий отказов).

### Задача B1. Золотой набор

Файлы: `docs/misc/col4-judge-goldenset-build.py`,
`docs/misc/col4-judge-golden.json`.

**Выполнена 2026-08-12**: 599 записей, детерминированная сборка
воспроизводима (`uv run python docs/misc/col4-judge-goldenset-build.py`,
вывод байт-стабилен). Состав: clean 166 (train 25 / dev 75 / test 66),
terminology 195 (23 / 95 / 77), mutation 238 (20 / 87 / 131).

Отклонения от плана и их основания:

- **Страта 1 — 195, а не 85.** Число 85 — свойство прод-состояния
  матчера (22 канонических термина без словоформ), а не корпуса:
  глоссарий dev-зеркала несёт 153 записи (31 термин + 122 формы),
  поэтому бакет `missed` там не воспроизводим — у семи терминов из
  таблицы п. 10 в нём **ноль** вхождений. Страта собрана той же
  логикой пробника с кэпом 8 на рендеринг; каждая запись несёт `term`
  и `matcher_bucket`, так что любое подмножество восстановимо.
  Точность метки по построению проверена вручную: **30/30**
  (Wilson 95% CI [88.6%, 100%]).
- **Классы мутаций.** `@@PHn@@` снят (0 вхождений в col4), порча
  буквенно-цифрового кода снята (0 кодовых токенов в чистом пуле).
  Добавлены наблюдённые в B0 `quote-frame-dropped` и
  `realia-decapitalised`; критический объём даёт отрицание инфинитива
  (`Ne pas <inf>`) — конвенция ANSWER-строк корпуса.
- **Гигиена генераторов.** Из антонимов исключены пары, управляющие
  разными предлогами (avec/sans, entrer/sortir, commencer/finir),
  снятие отрицания ограничено неэлидированным `ne`, детекция
  подлежащего защищена от клитик и относительных местоимений. Иначе
  мутация даёт грамматическую ошибку вместо смысловой инверсии —
  её судья ловит, не читая смысла, и reject-recall завышается.
- **Целостность сплитов.** Сплит закрепляется на уровне юнита: строка
  с двумя глоссарными дефектами не может попасть в dev одним и в test
  другим. Проверено: 0 юнитов в двух сплитах, 0 пересечений train с
  dev/test, 0 юнитов одновременно clean и terminology.

Мощность на test: critical n=94-90 — 95% даёт нижнюю границу CI 88-89%,
гейт `reject-recall` проверяем. **Не проверяем** продуктовый гейт
`auto-pass >= 90%`: чистых строк в test 66, false-flag 10% даёт
CI [5.2%, 20.3%]. Аргумент плана про n=150 относится к измеряемому
срезу, а сплит 15/45/40 оставляет в нём 40%. Решение (нужен выбор
заказчика): либо доразметить ~210 чистых строк, либо принять широкий
интервал и не заявлять «на порядок» числом из фазы 0.

~400 записей `{unit_id, source, target, label, defect_class,
annotator, split}`, три страты из прод-вывода COL4 fr:

1. **85 известных глоссарных дефектов** (разметка из
   `docs/specs/2026-08-11-glossary-enforcement-analysis.md`, п. 10)
   — класс `terminology`; метка по построению.
2. **~150 чистых строк**: 0 failing checks, глоссарий соблюдён, pass
   от разметчика (B0). 75 мало: порог false-reject <= 2% на n=75 даёт
   95% CI [0.7%, 9.2%] — непроверяем; n=150 даёт [0.7%, 5.7%].
3. **~180 синтетических мутаций** поверх чистых, детерминированный
   seed, метка по построению; >= 120 critical (>= 30 на класс) и
   >= 60 major. Классы — по сверке B0
   (`docs/misc/col4-judge-annotation.md`): critical — инверсия
   смысла/отрицание (подтверждён: 179849, 180322, 179557, 180800),
   непереведённый кириллический фрагмент (подтверждён: 180318,
   180681 — оба мимо чеков); major — подмена глоссарного термина
   (подтверждён богато: фрагментация реалий, ГИГАХРУЩ — 5+ вариантов),
   порча буквенно-цифрового кода В->B/Н->H (новый: 178484, 181049),
   смена лица tu/vous в нарративе (новый: 181347), вброс обсценности
   (новый: 180782), потеря числа (гипотетический — не встречен),
   пропуск куска (слабо подтверждён: потери кавычек-рамок).
   Порча/потеря `@@PHn@@` неприменима к col4 (0 вхождений в корпусе) —
   класс уходит в компоненты с плейсхолдерами. Идиоматические коллизии
   и потерю каламбуров не синтезировать — это few-shot улики промпта
   судьи. Размер продиктован мощностью:
   reject-recall >= 95% на ~15-20 critical неизмерим — идеальные 15/15
   дают 95% CI [79.6%, 100%]; на n=120 даже 114/120 держит нижнюю
   границу 89.5%.

Нормализация: перед разметкой каждый выгруженный `target` прогоняется
через `AUTOFIXES.fix_target()` (включая расширение автофикса терминала
на `!?:` из `2026-08-11-layer0-autofix-quick-wins.md`). Судью нельзя
калибровать на ~1443 детерминированных дефектах, которые слой 0 обязан
снять до него; in-flight нормализация даёт чистый набор, не дожидаясь
backfill на проде.

Сплиты против leakage (методология validate-evaluator):

- страты 1-2 (85 + 150): стратифицированно ~15% train (единственный
  источник few-shot для промпта судьи) / ~45% dev (итерации промпта) /
  ~40% test (запечатан, меряется ровно один раз перед go/no-go);
  числа dev как итог не репортятся — они оптимистичны;
- синтетика: dev-мутации seed A, test-мутации seed B на
  непересекающихся базовых строках; свежий test-срез регенерируем в
  любой момент сменой seed;
- постоянный регрессионный набор = train + dev; test вливается в
  регрессию после go/no-go, следующее существенное изменение промпта
  судьи требует свежего test-среза (новый seed + новая разметка).

### Задача B2. Прогон судей

Файлы: `docs/misc/col4-judge-eval.py`, результат в
`docs/LLM-first/2026-08-XX-judge-calibration.md`.

- Судьи: Anthropic mid-tier и OpenAI mini-tier через OpenRouter; точные
  ID фиксируются в результатах из каталога моделей на момент прогона.
  Оба семейства отличны от переводчика (`google/gemini-2.5-flash`) —
  инвариант 3 архитектуры.
- Промпт: MQM error-first, structured output
  `{errors: [{span, category, severity}], verdict}` — schema-enforced с
  первого прогона калибровки, тот же контракт, что пойдёт в прод
  (урок Cathedral: 19.6% вердиктов потеряны на свободном тексте).
  Глоссарий, note и `failing_checks` юнита в контексте — судья не
  пере-доказывает то, что код уже доказал. Вердикт из инвентаря:
  critical -> reject, major -> flag, иначе pass. Батч ~10 строк.
- Инвариант транспорта: неразобранный или пустой ответ судьи — сбой,
  не вердикт: ретрай, затем пропуск строки. В метриках калибровки
  считается отдельной колонкой (`unparsed`), не попадает ни в flag,
  ни в reject.
- Плечи: {2 модели} x {один прогон | 3x-агрегация на flag/reject}.
- Вердикт трёхзначный, но калибруются две независимые бинарные
  границы, каждая со своими TPR/TNR и разбором дисагриментов:
  граница R «есть critical» — reject против (flag + pass), защита
  релиза; граница H «нужен человек» — (flag + reject) против pass,
  confidence-роутер и главная продуктовая граница.
- Метрики: confusion matrix по классам; TPR/TNR границ R и H;
  reject-recall на critical-мутациях; (flag u reject)-recall на
  глоссарных дефектах; false-reject и false-flag на чистых; цена и
  латентность на 1000 строк.
- Продуктовая метрика: auto-pass rate >= 90% при скорректированном
  miss rate среди auto-pass (Rogan-Gladen по TPR/TNR границы H,
  bootstrap CI): critical ~ 0, major <= 1-2%. Точечные оценки без
  доверительного интервала не репортятся.
- Пороги go для фазы 2 — точечная оценка И нижняя граница 95% CI
  (Wilson): reject-recall (critical) >= 95% при CI >= 88% (тестируемо
  от n >= 120); (flag u reject)-recall (terminology) >= 80% (n=85,
  CI [70%, 87%] принимается — сигнал advisory); false-reject <= 2% на
  n=150; false-flag <= 10% на n=150. Терминологический recall < 80%
  -> терминология остаётся детерминированной (план морфологии), судья
  судит только семантику.
- Few-shot примеры промпта судьи — только из train-среза B1; итерации
  промпта — по dev; test меряется один раз, после него итерации по
  test запрещены (возврат — только со свежим срезом).
- Бюджет ~$8-15 (~400 записей x 2 модели x до 3x). Набор train + dev
  становится постоянным регрессионным для промпта судьи: любое
  изменение промпта/модели гоняется по нему, как изменения промпта
  переводчика — по eval-харнессу.

## Трек C. Пробник дрейфа не-глоссарных повторов

Файлы: `docs/misc/col4-repeat-drift-probe.py`, результат — раздел в
`docs/LLM-first/2026-08-XX-judge-calibration.md`.

Проверка переносимости правки 10.1 Cathedral (рантайм-канон
first_mentions, 23.4% их стадии правки) на нашу систему, где точные
повторы закрывает кэш machinery, термины — полный глоссарий <=300, а
детекцию — стоковый чек `inconsistent`:

- По прод-выводу COL4 fr сгруппировать повторяющиеся не-глоссарные
  фразы (n-граммы >= 3 символов, входящие в несколько юнитов) и
  посчитать долю групп с расходящимися переводами.
- Расхождение считается после нормализации (casefold, trim,
  схлопывание пробелов, снятие финальной пунктуации); репортятся обе
  цифры — raw и normalized, чтобы не раздувать «дрейф» косметикой,
  которую слой 0 снимает бесплатно.
- Дрейф материален -> рантайм-канон встаёт в фазу 2/4 отдельным
  дизайном (порядок батчей и concurrency ломают семантику канона —
  проектировать, не копировать). Дрейф незначим -> закрыто замером,
  как п. 10.4 анализа закрыл проверку порядка тегов.
- Read-only, без LLM-вызовов, бюджет $0.

## Вне объёма

- Запись вердиктов в Weblate, auto-approve, UI — фаза 2 и далее.
- Короткий промпт переводчика — отдельный эксперимент после схемы.
- CometKiwi и low-resource языки — фаза 4.
- Изменение прод-инстанса в любом виде.

## Порядок

A1 -> A2; B0 -> B1 -> B2; трек C независим. Треки идут параллельно, но
фаза 2 не стартует раньше завершения A (structured output — блокер,
не оптимизация) и B (калибровка).
