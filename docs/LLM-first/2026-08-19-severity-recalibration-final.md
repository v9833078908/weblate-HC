# Замеры: рекалибровка severity судьи (zh_Hans), полный прогон

Дата: 2026-08-19. Статус: **все три плеча A/B/C измерены на n=5 × 2 судьи;
вердикт по рубрике — ниже.**
Среда исполнения: хост VPS (прод-контейнер `hcgameloc-weblate-1` через
`deploy/vps.sh` использован только для read-only выборки ключа OpenRouter;
прогон шёл системным python3 на хосте VPS с троттлингом `--sleep 10`).
Данные корпуса read-only с прода, прод не менялся.

Основание: `docs/LLM-first/plans/2026-08-14-judge-severity-recalibration.md`.
Постановка и заготовки: `docs/LLM-first/2026-08-18-severity-recalibration-status.md`.
Скоринг воспроизводим: `docs/misc/st2-zh-score.py`
(`python3 st2-zh-score.py --truth st2-zh-groundtruth.json --out-dir st2-zh-recal`).

## Корпус и плечи

124 юнита `strategy-and-tactics-2/summer-update` zh_Hans, все state 20.
Плечи (все — с глоссарием и правилом «conformance necessary but never
sufficient», вопрос глоссария закрыт 2026-08-14):

- **A** — текущий промпт (baseline);
- **B** — A + обязательный `description` (обратный перевод для продюсера);
- **C** — B + рубрика severity «последствие для игрока».

Судьи: `deepseek/deepseek-v4-pro` (место 1), `qwen/qwen3-235b-a22b-2507`
(место 2). По 5 повторов на (плечо × судьи), все прогоны 124/124 распарсены.

## Эталон (ground truth)

`docs/misc/st2-zh-groundtruth.json`: **7 critical, 17 major, 17 minor,
83 none**. Построен из человеческой разметки (14-дефектный якорь +
инвентарь исправленной выгрузки, severity по рубрике последствия для
игрока). 24130 и 24194 помечены спорными. Модельные аннотаторы не годятся
как единственный эталон (воспроизводят слепые пятна судей — 9 из 14
подтверждённых дефектов занижены; детали в статус-доке 2026-08-18).

## R0: шумовой пол на n=5 (внутриплечевой разброс)

Доля юнитов, у которых статус меняется между 5 повторами:

| Плечо | Конфигурация | flip ≥flag | any-severity |
|---|---|---|---|
| A | `deepseek` | 36/124 (29%) | 45/124 |
| A | `qwen` | 27/124 (22%) | 35/124 |
| A | коллегия | 39/124 (31%) | 56/124 |
| B | `deepseek` | 40/124 (32%) | 50/124 |
| B | `qwen` | 25/124 (20%) | 38/124 |
| B | коллегия | 36/124 (29%) | 56/124 |
| C | `deepseek` | 22/124 (18%) | 32/124 |
| C | `qwen` | 31/124 (25%) | 46/127 |
| C | коллегия | 30/124 (24%) | 51/124 |

Шумовой пол 20-32% — любой диф между плечами меньше этого не значим.
(R0 пере-подтверждён: считать все пороги от n=5.)

## Медианы по коллегии (max severity двух мест)

| Плечо | missed_crit | false_crit | REAL@24 | FP (none→≥major) | prec@≥major | rec@≥major | noise ≥flag |
|---|--- продюсера видит обоснование каждого флага, и это единственное плечо,
где FP deepseek упал вдвое (8→4). REAL@24 у арма B — 16, минимальный из
трёх (recall на 24 дефектных юнитах ниже, чем у baseline).
|---|
