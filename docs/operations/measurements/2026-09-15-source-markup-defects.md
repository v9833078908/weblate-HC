# source markup: замер правила закрывающего тега с атрибутом, 2026-09-15

Правило `source.tag_closing_has_attribute` из плана
`docs/product/plans/2026-09-11-loc-kit-source-validation.md`: закрывающий
Unity-тег (`</color>`, `</sprite>`, …) несёт `=` или пробельный атрибут,
например `</color=yellow>` из дефекта 525591. Проба:
`analysis/probes/source-markup-defects.py`, артефакты прогона:
`analysis/data/source-markup-defects-2026-09-15.json` (dev),
`analysis/data/source-markup-defects-2026-09-15-prod.json` (production).

## Прогон на dev-инстансе

Инстанс: `http://localhost:3001` (локальный dev-стек). Проба read-only:
проекты перечислены через `/api/projects/`, для **каждого** компонента взят
его собственный `source_language`, исходная транзакция пролистана курсором
(`?page_size=1000`, переход по `next` до конца), источник обработан один раз
на `(component, context)`.

| Проект | Компонентов | Source units | Кандидатов |
|---|---|---|---|
| space-arena | 2 | 5126 | 0 |
| need-for-greed | 8 | 886 | 0 |
| pirate-ships | 3 | 3727 | 0 |
| heart-abyss | 2 | 700 | 0 |
| col4 | 3 | 4024 | 0 |
| test | 2 | 1 | 0 |
| judge-repair-probe | 2 | 124 | 0 |
| fixcheck-sandbox | 2 | 8 | 0 |
| **итого** | **24** | **14596** | **0** |

Кандидатов не найдено. Отсутствие находок — корректный результат; он не даёт
ни ложных срабатываний, ни оснований расширять правило.

## Проверка детектирующего пути

Живой прогон не содержит ни одного позитивного примера, поэтому
детектирующий путь пробы (пагинация, дедупликация, счётчик, примеры)
проверен отдельным smoke с замоканным API: две страницы юнитов, повторный
`(component, context)`, строка 525591, чистая строка и строка с двумя
независимыми битыми тегами. Проба следовала курсору на вторую страницу,
обработала 3 юнита вместо 4 (дедуп сработал), вернула 3 кандидата и три
примера. Данные инстанса при этом не менялись.

## Производственный замер, 2026-09-15

Read-only прогон по `https://l10n.herocraft.com/api` выполнен в тот же день с
разрешения владельца инстанса; токен — `PROD_WEBLATE_API_TOKEN`
(`deploy/.env.local`, доступ только на чтение). Проба перечислила все проекты,
для каждого компонента взяла его собственный `source_language` и пролистала
исходную транзакцию курсором `?page_size=1000`. Путь до инстанса рвёт
TLS-рукопожатия спорадически; ретраи пробы (5 попыток, до 62 с на запрос)
перекрыли два таких обрыва без повторного запуска.

| Проект | Компонентов | Source units | Кандидатов |
|---|---|---|---|
| col4 | 3 | 4118 | 0 |
| pirate-ships | 2 | 4059 | 0 |
| heart-abyss | 3 | 1305 | 0 |
| strategy-and-tactics-2 | 2 | 684 | 0 |
| need-for-greed | 9 | 1222 | 0 |
| space-arena | 2 | 5210 | 0 |
| victory-banner | 2 | 693 | 0 |
| anvil-saga | 2 | 9666 | **1** |
| dead-shell | 2 | 1160 | 0 |
| test-project-pi | 2 | 234 | 0 |
| curse-of-pirates | 0 | 0 | 0 |
| chronocracy | 0 | 0 | 0 |
| **итого** | **24** | **28351** | **1** |

Единственный кандидат — true positive, классификация вручную:

- `anvil-saga` / `locale_v1-02-import-explained-3`, контекст `SpawnRate`:
  `Заказчики приходят в </color=yellow>{0}</color> раза реже.` — это дефект
  525591 из аудита Anvil Saga, открывающий тег написан как закрывающий.
  Ложных срабатываний нет: precision на производственном корпусе 1/1.

Курсорное покрытие полное: сумма `source_units` по таблице равна общему
числу просмотренных юнитов (28 351), пагинация следовала `next` до конца;
два проекта без компонентов (`curse-of-pirates`, `chronocracy`) вошли в
артефакт с нулями.

## Гейт Task 2 (включение на production)

Отчёт Task 0 закрыт: правило находит подтверждённый дефект и не даёт
ложных срабатываний ни на dev (14 596 юнитов), ни на production
(28 351 юнит). Включение `GameSourceMarkupCheck` на production — это
deployment, который требует отдельного разрешения владельца; само
разрешение применения проверки этим документом не выдаётся.
