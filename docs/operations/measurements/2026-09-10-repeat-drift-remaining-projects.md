# repeat-drift: правка судьи на прод и раскатка на все проекты, 2026-09-10

Инстанс: `l10n.herocraft.com` (`hc-srv15-localizer`). Ревизия после выкатки:
`0c69923`, `DEPLOY-OK`, healthy за 172 с, login 200, миграций нет.

## Исходное состояние (поправка к более раннему выводу)

Первая версия этого замера утверждала, что `repeat-drift` включён только на
`need-for-greed`. Это неверно. Аудит `Change.target="check_flags"` на проде:

| Когда | Кто | Проект |
|---|---|---|
| 2026-09-03 20:12 | (скрипт) | need-for-greed |
| 2026-09-04 11:13 | i.efimov@herocraft.com | heart-abyss |
| 2026-09-04 11:15 | (скрипт) | col4, victory-banner, strategy-and-tactics-2 |

То есть пять проектов уже несли флаг и посчитанные строки. Вывод «включён
один проект» был получен чтением плана и роллаут-документа, а не состоянием
инстанса; заголовки документов отстали от прода на неделю. Состояние прода
проверяется запросом к проду.

## Что выкачено

`ee3ab54 fix(judge): keep repeat-drift out of the judge prompt`. До правки
`build_request` слал судье `all_checks_names - JUDGE_CHECKS`, куда входил
`repeat-drift`; промпт вердикта трактует любой перечисленный чек как
доказанный кодом дефект. Чек срабатывает на всех членах группы одинаковых
исходников, включая правильный перевод, поэтому судья получал непрозрачное
«доказательство» и мог промолчать о реальной находке. MT/repair-путь
(`llm.py`) этот чек отбрасывал давно; теперь фильтры симметричны.

Проверено в работающем контейнере:
`unit.all_checks_names - JUDGE_CHECKS - {REPEAT_DRIFT_CHECK_ID}`.

Попутно уехали `cc5aacc` (loc-kit) и два измерительных коммита scene-context,
не трогающих продкод.

## Что включено

Флаг `repeat-drift` на уровне проекта плюс батч-проход
`CHECKS["repeat-drift"].perform_batch(component)` — точечный путь вместо
`updatechecks`, который пересчитывает все чеки по всем юнитам. Чек включён
на **всех десяти проектах инстанса**.

| Проект | Строк | Групп | Макс. групп в языке | Время прохода | Предсказание пробы |
|---|---|---|---|---|---|
| anvil-saga | 2783 | 833 | 200 (потолок) | 3,5 с | 804 |
| pirate-ships | 2071 | 811 | 68 | 24,2 с | 762 |
| space-arena | 1607 | 694 | 113 | 79,7 с | 552 |
| col4 | 621 | 245 | 76 | — | 243 |
| need-for-greed | 322 | 145 | 24 | — | — |
| heart-abyss | 255 | 126 | 23 | — | 126 |
| korotkij-test | 140 | 63 | 10 | 1,3 с | 0 |
| dead-shell | 136 | 62 | 10 | 0,1 с | не измерялся |
| victory-banner | 14 | 6 | 6 | — | 1 |
| strategy-and-tactics-2 | 2 | 1 | 1 | — | 1 |
| **итого** | **7951** | | | | |

Проба `analysis/probes/source-repeat-drift.py` подтвердилась как предсказатель
на крупных проектах (heart-abyss точно, col4 245 против 243, pirate-ships 811
против 762, anvil-saga 833 против 804) и промахнулась на `korotkij-test`, где
не выбрала юниты вовсе.

`dead-shell` дал 0 строк на проходе в 13:44 и 136 при сверке в 14:05: проект
в это время наполняли, и строки создал обычный несбатченный путь
`check_target_unit` при сохранении юнитов. Это штатное поведение, а не
результат повторного прохода.

## Потолок батча: сработал только на anvil-saga

`RepeatDriftCheck.batch_limit = 200` ограничивает проход двумястами группами
**на plural**, а `Plural` в Weblate привязан к языку, поэтому предел
действует по языкам.

- **anvil-saga** — упёрся, как и предсказывала проба (`ja` 220 групп, `ko`
  205). Прод подтвердил ровно эти два plural:

  ```text
  repeat-drift: hit the 200 group cap on anvil-saga (plural 344)
  repeat-drift: hit the 200 group cap on anvil-saga (plural 383)
  ```

- **space-arena** — проба показывала четыре языка сверх предела (`vi` 285,
  `th` 280, `id` 263, `hi` 243), но потолок не сработал: эти числа включали
  869 групп на стыке глоссария и обычных компонентов, а чек глоссарные юниты
  исключает. После исключения максимум по языку — 113 групп.
- **pirate-ships** — максимум 68 групп в языке, запаса втрое.

`_perform_batch` удаляет строки для юнитов, до которых проход не дошёл, и
порядок обхода детерминирован, поэтому повторный проход даёт то же
множество: это не накопление, а усечение. Примерно 25 групп в `ja` и `ko`
на anvil-saga станут видимы после того, как будут разобраны первые 200. Для
advisory-детектора это приемлемо; менять `batch_limit` без отдельного замера
стоимости прохода не следует.

## Не сделано

- Парный прогон судьи «с флагом / без флага»: риск подавления прослежен по
  коду и промпту, дельта вердиктов не измерена.
- Разбор находок: чек — детектор, он ничего не чинит и ничего не блокирует.
- `audit_glossary` на space-arena: 869 групп расхождений живут на стыке
  глоссария и обычных компонентов и в `repeat-drift` не попадают по
  построению.
